<!DOCTYPE html>
<html lang="en">
  <%- include('partials/scripts.ejs') %>
  <body>
    <%- include('partials/usersDBHeader.ejs') %>
    <div class="container">
      <div class="row">
        <div class="col">
          <table class="table table-striped">
            <tr>
              <th>Username</th>
              <th>age</th>
              <th>Is this user Admin</th>
            </tr>

            <% for(let i=0; i < users.length; i++) { 
                const user = users[i]
            %>
            <tr>
              <td><%= user.username %></td>
              <td><%= user.age %></td>
              <td><%= user.isAdmin %></td>
            </tr>
            <% } %>
          </table>
        </div>
      </div>
    </div>
  </body>
</html>